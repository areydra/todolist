export default class Todolist{
    constructor(id, title, category, date, status){
        this.id = id
        this.title = title,
        this.category = category,
        this.date = date,
        this.status = status
    }
}